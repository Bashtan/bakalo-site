# macOS
.DS_Store

# Claude Code internals (transcripts, state) — commit project settings only
.claude/*
!.claude/settings.json

# Node
node_modules/
worker/node_modules/

# Wrangler
.wrangler/
worker/.wrangler/
wrangler.toml.bak

# Local secrets — never commit
.env
.env.local
.cf.env
