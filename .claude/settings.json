{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "CMD=$(jq -r '.tool_input.command // empty' 2>/dev/null); if echo \"$CMD\" | grep -qE 'git push[^|;&]*\\b(origin|upstream)\\b[^|;&]*\\bmain\\b'; then printf '{\"decision\":\"block\",\"reason\":\"Staging-first rule: always push to fix/<name> branch for staging preview. Deploy to production (main) ONLY after explicit user confirmation in chat.\"}'; fi"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "HANDOFF=$(git -C /Users/bashtan/Projects/bakalo-site log -1 --format='%H' -- HANDOFF.md 2>/dev/null); HEAD=$(git -C /Users/bashtan/Projects/bakalo-site rev-parse HEAD 2>/dev/null); if [ -n \"$HEAD\" ] && [ \"$HANDOFF\" != \"$HEAD\" ]; then echo \"HANDOFF.md is behind HEAD. Update it to reflect recent work, then commit it.\"; exit 2; fi",
            "asyncRewake": true,
            "rewakeSummary": "HANDOFF.md needs updating"
          }
        ]
      }
    ]
  }
}
